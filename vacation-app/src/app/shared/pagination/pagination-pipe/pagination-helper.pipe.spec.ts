import { PaginationHelperPipe } from './pagination-helper.pipe';

describe('PaginationHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
